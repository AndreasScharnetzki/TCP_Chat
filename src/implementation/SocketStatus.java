package implementation;

public enum SocketStatus {

    //These enums shall be used as return values for status request of TCP-Peers socket

    CONNECTED,

    CLOSED,

    //used if not initialized
    UNDEFINED;
}
